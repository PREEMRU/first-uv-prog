[project]
name = "hexlet-hello-world"
version = "0.1.0"
description = "first uv programm"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "more-itertools>=11.0.2",
]

[dependency-groups]
dev = [
    "ruff>=0.15.12",
]
